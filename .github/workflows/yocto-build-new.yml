name: Yocto build (frankenpi - new)

on:
  push:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-22.04
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Print hello
        run: |
          echo "This is a minimal test workflow for frankenpi."
          echo "If this runs, GitHub accepts workflows from this repo."

